[build]
  publish = "."
  functions = "netlify/functions"

# Redirect /api/state to the serverless function — keeps frontend code unchanged
[[redirects]]
  from = "/api/state"
  to = "/.netlify/functions/state"
  status = 200
